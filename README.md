## Общая информация

В этом репозитории содержится серверная часть проекта для дисциплины "Визуальное программирование". Клиентская часть располагается в смежном репозитории "mobiledev."

В папке external содержатся сторонние библиотеки, необходимые для работы программы, в частности, imgui и implot. Добавлены сабмодулем.

В папке src находится код проекта, main.cpp. В нём содержится и логика общения с клиентом, и графическая оболочка программы.

Папка build внесена в .gitignore, инструкция по сборке программы приведена ниже. 

## Сборка проекта

В корне проекта:

```bash
mkdir build && cd build
cmake ../
make -j16 #16 - количество потоков. Можно ставить иное число
```

## Логика работы main.cpp

Структура TelemetryData содержит в себе всю передаваемую с телефона информацию.

```C++
struct TelemetryData {
    float lat = 0, lon = 0, alt = 0, acc = 0;
    int rsrp = 0;
    string type = "";
    mutex mtx; 
    string raw = "";
} g_data;
```

Функция `string find_val(string json_text, string key)` нужна для парсинга и работы с получаемым json.

Функция `void zmq_server()` реализует соединение с клиентом и получение от него информации. Также, отвечает клиенту 'OK' для подтверждения соединения.

В главной функции `int main(int argc, char *argv[])` под `zmq_server()` выделяется отдельный mutex. Imgui в связке с Implot создают окно, в котором выводят основные данные, плюс сырой json для опциональной информации.

## TODO

 - Планируется вынести логику интерфейса в отдельную функцию, а в main оставить лишь запуск потоков на zmq и на интерфейс. (на данный момент реализовано только под zmq, интерфейс работает в main функции). 

 - Планируется подтянуть базу данных PostgreSQL для записи полученных с клиента данных. Соответственно, и функции для работы с БД.

  - Планируется добавить отображение карты, а также точек на ней с возможностью клика для детальной информации о каждой записанной локации.

  - Планируется изменить цветовую гамму проекта, чтобы он отличался от остальных студентов.
